---
// Expandable Feature Cards Component - 4 Core Features
---

<div class="grid grid-cols-2 features-grid">
  <!-- Feature 1: Dynamic Offer Creation -->
  <div class="card expandable-card" data-card-id="dynamic-offers">
    <div class="card-header">
      <div class="card-icon">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M12 2L2 7L12 12L22 7L12 2Z"/>
          <path d="M2 17L12 22L22 17"/>
          <path d="M2 12L12 17L22 12"/>
        </svg>
      </div>
      <h3 class="card-title">Dynamic Offer Creation</h3>
      <p class="card-text">Create personalized offers in real-time based on customer preferences and behavior</p>
      <button class="expand-btn" aria-label="Expand details">
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M4 6l4 4 4-4"/>
        </svg>
      </button>
    </div>
    <div class="card-expanded-content">
      <h4>How Dynamic Offers Work</h4>
      <ul>
        <li><strong>AI-Powered Personalization:</strong> Machine learning analyzes customer behavior, booking history, and preferences to create tailored offers</li>
        <li><strong>Real-Time Pricing:</strong> Dynamic pricing engine adjusts offers based on demand, inventory, and customer segments</li>
        <li><strong>Smart Bundling:</strong> Contextual recommendations increase ancillary revenue by suggesting relevant add-ons</li>
        <li><strong>NDC-Native:</strong> Built on IATA NDC standards for modern airline retailing and distribution</li>
      </ul>
      <p class="detail-note">Enables airlines to compete with OTAs by offering personalized, differentiated products</p>
    </div>
  </div>

  <!-- Feature 2: Instant Order Processing -->
  <div class="card expandable-card" data-card-id="order-processing">
    <div class="card-header">
      <div class="card-icon">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
          <polyline points="14 2 14 8 20 8"/>
          <line x1="16" y1="13" x2="8" y2="13"/>
          <line x1="16" y1="17" x2="8" y2="17"/>
        </svg>
      </div>
      <h3 class="card-title">Instant Order Processing</h3>
      <p class="card-text">Lightning-fast order management with seamless modifications and updates</p>
      <button class="expand-btn" aria-label="Expand details">
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M4 6l4 4 4-4"/>
        </svg>
      </button>
    </div>
    <div class="card-expanded-content">
      <h4>Order Management Excellence</h4>
      <ul>
        <li><strong>Sub-100ms Processing:</strong> Edge computing with global CDN nodes ensures instant order confirmation</li>
        <li><strong>ONE Order Standard:</strong> Single order record for all services—flights, seats, meals, baggage, and more</li>
        <li><strong>Seamless Modifications:</strong> Change flights, add services, or cancel bookings without re-booking or complex workflows</li>
        <li><strong>Real-Time Synchronization:</strong> Order updates instantly reflected across all channels—web, mobile, call center, airport</li>
      </ul>
      <p class="detail-note">Process 10K+ orders per second with 99.99% uptime SLA</p>
    </div>
  </div>

  <!-- Feature 3: Real Revenue Uplift -->
  <div class="card expandable-card" data-card-id="revenue-uplift">
    <div class="card-header">
      <div class="card-icon">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <polyline points="23 6 13.5 15.5 8.5 10.5 1 18"/>
          <polyline points="17 6 23 6 23 12"/>
        </svg>
      </div>
      <h3 class="card-title">Real Revenue Uplift</h3>
      <p class="card-text">Proven results with measurable increases in conversion and ancillary revenue</p>
      <button class="expand-btn" aria-label="Expand details">
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M4 6l4 4 4-4"/>
        </svg>
      </button>
    </div>
    <div class="card-expanded-content">
      <h4>Measurable Business Impact</h4>
      <ul>
        <li><strong>12-15% Revenue Increase:</strong> Airlines see higher total revenue per booking through personalized offers and upsells</li>
        <li><strong>23% Ancillary Boost:</strong> Smart bundling and contextual recommendations drive significant ancillary revenue growth</li>
        <li><strong>18-35% Conversion Lift:</strong> A/B testing shows dramatic improvement in checkout completion rates vs. traditional systems</li>
        <li><strong>Real-Time Analytics:</strong> Live dashboards track revenue, conversion funnels, and booking patterns for data-driven decisions</li>
      </ul>
      <p class="detail-note">Based on aggregated customer data across 5M+ orders processed over 12 years</p>
    </div>
  </div>

  <!-- Feature 4: Customer-Centric Design -->
  <div class="card expandable-card" data-card-id="customer-centric">
    <div class="card-header">
      <div class="card-icon">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
          <circle cx="12" cy="7" r="4"/>
        </svg>
      </div>
      <h3 class="card-title">Customer-Centric Design</h3>
      <p class="card-text">Built around customer needs and experiences, not legacy systems</p>
      <button class="expand-btn" aria-label="Expand details">
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M4 6l4 4 4-4"/>
        </svg>
      </button>
    </div>
    <div class="card-expanded-content">
      <h4>Customer-First Philosophy</h4>
      <ul>
        <li><strong>Unified Customer Profile:</strong> Single view across all touchpoints—web, mobile, call center, airport, and partner channels</li>
        <li><strong>Preference Memory:</strong> System remembers seat preferences, meal choices, loyalty tier, and special requirements</li>
        <li><strong>Frictionless Experience:</strong> Simplified booking flows reduce abandonment and improve customer satisfaction</li>
        <li><strong>Proactive Service:</strong> Automated notifications for delays, gate changes, and relevant upsell opportunities at the right moment</li>
      </ul>
      <p class="detail-note">Designed using IATA ONE Order principles for true customer-centric airline retailing</p>
    </div>
  </div>
</div>

<style>
  .features-grid {
    gap: 2rem;
  }

  .expandable-card {
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .expandable-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.12);
  }

  .card-header {
    position: relative;
  }

  .expand-btn {
    position: absolute;
    top: 1rem;
    right: 1rem;
    background: transparent;
    border: none;
    cursor: pointer;
    padding: 0.5rem;
    color: var(--color-primary, #0a5c5c);
    transition: transform 0.3s ease;
    animation: bounce-hint 2s ease-in-out infinite;
  }

  @keyframes bounce-hint {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(3px);
    }
  }

  .expandable-card:hover .expand-btn {
    animation: none;
    transform: translateY(2px);
  }

  .expandable-card.expanded .expand-btn {
    animation: none;
  }

  .expandable-card.expanded .expand-btn svg {
    transform: rotate(180deg);
  }

  @media (prefers-reduced-motion: reduce) {
    .expand-btn {
      animation: none;
    }
  }

  .card-expanded-content {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s ease, padding 0.4s ease;
    padding: 0 1.5rem;
  }

  .expandable-card.expanded .card-expanded-content {
    max-height: 1000px;
    padding: 0 1.5rem 1.5rem 1.5rem;
    border-top: 1px solid #e5e7eb;
    margin-top: 1rem;
  }

  .card-expanded-content h4 {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-primary, #0a5c5c);
    margin: 1rem 0 0.75rem 0;
  }

  .card-expanded-content ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .card-expanded-content li {
    margin-bottom: 0.75rem;
    padding-left: 1.5rem;
    position: relative;
    line-height: 1.6;
    color: #4b5563;
  }

  .card-expanded-content li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--color-accent, #f5b800);
    font-weight: bold;
  }

  .card-expanded-content li strong {
    color: #1f2937;
    font-weight: 600;
  }

  .detail-note {
    margin-top: 1rem;
    padding: 0.75rem 1rem;
    background: #f9fafb;
    border-left: 3px solid var(--color-accent, #f5b800);
    font-size: 0.875rem;
    color: #6b7280;
    font-style: italic;
  }

  /* 2-column grid on desktop, 1 column on mobile */
  @media (max-width: 768px) {
    .features-grid {
      grid-template-columns: 1fr !important;
      gap: 1.5rem;
    }

    .expandable-card.expanded .card-expanded-content {
      max-height: 1500px;
    }

    .card-expanded-content li {
      font-size: 0.875rem;
      padding-left: 1.25rem;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const cards = document.querySelectorAll('.expandable-card');

    cards.forEach(card => {
      card.addEventListener('click', (e) => {
        // Don't toggle if clicking on a link
        if ((e.target as HTMLElement).tagName === 'A') {
          return;
        }

        // Close other expanded cards
        cards.forEach(otherCard => {
          if (otherCard !== card && otherCard.classList.contains('expanded')) {
            otherCard.classList.remove('expanded');
          }
        });

        // Toggle current card
        card.classList.toggle('expanded');
      });
    });
  });
</script>
